function combinandoNomesPokemon(palavra) {
    let palavraPokemon = palavra + "saur";
    return (palavraPokemon)
}

var nomeEntrada = gets();

var palavraGerada = combinandoNomesPokemon(nomeEntrada);

console.log(palavraGerada);